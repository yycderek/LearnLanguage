# 安全政策

## 报告漏洞

请不要为尚未修复的漏洞创建公开 Issue。使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能，提供：

- 受影响版本或提交；
- 可复现步骤和最小样例；
- 可能影响的数据与攻击条件；
- 已知缓解办法。

维护者会尽快确认收到报告，在评估完成前保持私密，并在修复可用后协调披露。项目不承诺漏洞奖励。

## 安全边界

- API Key 仅应保存在当前标签页，不应提交或同步。
- 课程和语言包是数据，不允许携带可执行第三方 JavaScript。
- AI 输出不可信，必须通过结构校验并由用户审核。
- 可选同步服务不得成为离线学习的单点依赖。
- 自部署者负责 TLS、身份验证、备份、速率限制和运行环境安全更新。
